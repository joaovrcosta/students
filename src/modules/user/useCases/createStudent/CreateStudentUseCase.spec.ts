
describe("Criar categoria", () => {
    it("Espero que 2 + 2 seja 4", () => {
        const soma = 2 + 2
        const resultado = 4

        //para rodar é yarn test
        expect(soma).toBe(resultado)
    });
});